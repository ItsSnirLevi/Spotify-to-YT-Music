# ===== Spotify (REQUIRED) =====
SPOTIPY_CLIENT_ID=your_spotify_client_id
SPOTIPY_CLIENT_SECRET=your_spotify_client_secret
# Use loopback IP (not "localhost") to avoid deprecation warnings:
SPOTIPY_REDIRECT_URI=http://127.0.0.1:8080/callback

# ===== YouTube Music OAuth (REQUIRED) =====
# OAuth client created as "TVs and Limited Input devices"
YTMUSIC_CLIENT_ID=your_google_oauth_client_id
YTMUSIC_CLIENT_SECRET=your_google_oauth_client_secret
# Path to the oauth.json generated by: ytmusicapi oauth --client-id ... --client-secret ... --oauth-file oauth.json
YTMUSIC_OAUTH=./oauth.json

# ===== Script Options (Optional) =====
# Comma-separated playlist names; leave empty to process all (including "Liked Songs")
INCLUDE_PLAYLISTS=
EXCLUDE_PLAYLISTS=
# Set to 1/true/yes to only compare (no modifications)
COMPARE_ONLY=
